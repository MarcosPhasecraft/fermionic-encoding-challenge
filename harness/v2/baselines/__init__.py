"""Ancilla/stabilizer-aware reference baselines for the v2 extension.

Not the frozen top-level baselines/ directory (which registry.json/the
legacy leaderboard manage) -- these are ancilla-format submissions
(n_qubits > M, non-empty stabilizers), tested only through
harness.v2.evaluate/harness.v2.challenges, never registered anywhere.
"""
