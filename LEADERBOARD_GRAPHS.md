# Leaderboard -- beyond square lattices

Generated by `scripts/update_leaderboard.py` — **do not hand-edit this file**. A separate challenge from the square-lattice one in `LEADERBOARD.md`: the target graphs are the 2D lattice types from arXiv 2504.21636's Table II (hexagonal, triangular, and their periodic variants, not square grids), scored under the exact same `D = Num + ReHop + ImHop + Inter` metric as the square-lattice challenge. Same non-negotiable rule as the square-lattice challenge: the harness does not search orderings on a submission's behalf; each graph type ships one canonical default ordering (see `harness/graphs.py`), overridable by a submission's own declared `order(Lx, Ly) -> perm` exactly as today.

A submission declares which shape(s) it targets as explicit `Lx x Ly` pairs (e.g. `"sizes": "8x4,15x15"` in `submission.json` -- see `inbox/README.md`), not a single size: for these lattice types, mode count `M` alone does **not** determine the graph -- different `(Lx, Ly)` splits at the same `M` are structurally different graphs (see `harness/graphs.py`'s `CANONICAL_SHAPE`). Each graph type has exactly one shape pinned as directly comparable to the paper's own numbers (the "vs. Table II" section below); a submission at any other shape still gets verified, scored, and cached, and shows up in the "Other shapes" section, just not against `[1]`. `[1]` rows are arXiv 2504.21636's own published Table II -- our own construction of the canonical shape won't reproduce those numbers exactly (the paper doesn't state the exact shape it used), only land in the same ballpark; see NOTES.md.

Lower is better, on both columns.

## Hex-Lattice

`D = Num + ReHop + ImHop + Inter`

### vs. Table II (8x4)

| label | shape | total weight | max weight |
|---|---|---|---|
| TT [1] | 8x4 ref. | **5489** | -- |
| JW [1] | 8x4 ref. | **7564** | -- |

## Tri-Lattice

`D = Num + ReHop + ImHop + Inter`

### vs. Table II (8x8)

| label | shape | total weight | max weight |
|---|---|---|---|
| JW [1] | 8x8 ref. | **2384** | -- |
| TT [1] | 8x8 ref. | **2478** | -- |

## Periodic Hex-Lattice

`D = Num + ReHop + ImHop + Inter`

### vs. Table II (8x4)

| label | shape | total weight | max weight |
|---|---|---|---|
| TT [1] | 8x4 ref. | **4794** | -- |
| JW [1] | 8x4 ref. | **8584** | -- |

## Periodic Tri-Lattice

`D = Num + ReHop + ImHop + Inter`

### vs. Table II (8x8)

| label | shape | total weight | max weight |
|---|---|---|---|
| TT [1] | 8x8 ref. | **2356** | -- |
| JW [1] | 8x8 ref. | **2704** | -- |

## References

[1] Chiew, Ibrahim, Safro, Strelchuk, *Optimal fermion-qubit mappings via quadratic assignment*, [arXiv 2504.21636](https://arxiv.org/abs/2504.21636), Table II.
